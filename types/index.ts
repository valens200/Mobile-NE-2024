export interface Product {
    id: string;
    productName: string;
    description: string;
    cost: number;
}

export interface User {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
}